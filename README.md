# Akira's Software Configs

This repo tracks my settings under `~/.config/`.

## Quick Install

```sh
mv ~/.config ~/.config.backup
git clone git@github.com:akirabaruah/configs.git ~/.config
cp -rT ~/.config.backup ~/.config
rm -rf ~/.config.backup
```
